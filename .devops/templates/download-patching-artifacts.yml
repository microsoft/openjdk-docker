parameters:
  - name: organization
    type: string
  - name: feed
    type: string

steps:
  - template: /.devops/templates/download-artifact.yml@self
    parameters:
      organization: ${{ parameters.organization }}
      feed: ${{ parameters.feed }}
      package: trivy-linux-x64
      version: $(TRIVY_VERSION)

  - template: /.devops/templates/download-artifact.yml@self
    parameters:
      organization: ${{ parameters.organization }}
      feed: ${{ parameters.feed }}
      package: copa-linux-x64
      version: $(COPA_VERSION)
